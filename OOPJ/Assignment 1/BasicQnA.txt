1. What is Java?
-> java is an High level Object oriented Programming Language use to develope
   various services and Applications, Developed by Sun Microsystem in 1995.

2. Is Java platform dependent or independent?
-> Java is Platfrom Independent but its JVM that is run time environment is
   is Platform Dependent.

3. What is JVM?
-> JVM stands for Java Virtual Machine. It is used to run java programs on different
   machines. It convert Byte code into machine code and thats why it is platform dependent.

4. What is JDK and JRE?
-> JDK is Acronym for JAVA Development Kit it is used to develope complete application.
   It has compiler, Debugger, library classes, JRE, JVM to run code.
   JRE is acronym for Java Runtime Environment is a software that provides necessary
   libraries and JVM to run Java Program.

5. Why is Java called “Write Once, Run Anywhere”?
-> In java code compile by compiler is converted into Byte code which is not machine specific
   code and after that Byte code is then converted into machine code by JVM which is platform 
   Dependent. So that any byte code of java can be run on any machine using that machines JVM.

6. What is a class in Java?
-> Class is blueprint or template by using we can create logical/physical entity.

7. What is an object?
-> It is an instance of a class i.e physical/logical entity.

8. What is the main method in Java?
-> main method in java is used to initialize a program/ it is entry point to start an execution of program.

9. What is the use of System.out.println()?
-> It is used to show output on a screen.

11. What are data types in Java?
->  It specify that what type of data we can store inside variable. In Java there are mainly two types of Data
    1) Primitive Data type    
    2) Non Primitive Data type 

12. Give examples of primitive data types
->  byte, short, int, long, float, double, char, boolean.

13. What is a variable?
-> It is a name given to a memory location address.

14. How do you declare a variable in Java?
->  int num = 10;
    here num is a variable name, int is a primitive data type and 10 is value assign to it.

15. What is the difference between int and float?
->  into stores normal integer but float stores decimals. By default type of int is integer but type of float is double.

16. What is type casting?
->  Converting one data type into another data type.

17. What is the default value of int?
->  0 only for instance and static variable.

18. What is a keyword in Java?
->  It is predefined word in java which has specific meaning to compiler

19. Can we use reserved keywords as variable names?
-> No

20. What is the difference between = and ==?
-> '=' is used to assign a value and '==' is used for equality check.

21. What is an if statement?
->  It is a conditional statement used to check the condition. It allows to execute specific block of code
    only if certain condition is true.
 
22. Difference between if and switch?
->  If     : 1) Check condition to run code
	     2) if statement can check ranges
	     3) Works with any data type
	     4) Evalutes condition one by one until find a match
	     5) Uses an else block at the end

    Switch : 1) Takes an expression and executes the matching case for that expression
	     2) Limited to int, char, short, byte, String, enums
	     3) Much easier to read for multiple specific condition
	     4) Uses a jump table, which can be faster for many cases
	     5) Uses a default case at the end


23. What is a loop?
->  It is repetation of a program/code. Used to repeat specific block of code multiple times.

24. Types of loops in Java?
->  1) for loop
    2) while loop
    3) do while loop

25. Difference between for loop and while loop
->  For loop  : 1) if we know no of iteration then use for loop
		2) Syntax = for(int i =0 ; i<10; i++){ //Code }
		3) counter variable usually local to the loop

    Wile loop : 1) If we don't know iteration of a program then use while loop
		2) Syntax = while(condition) {}
		3) counter variable is declared outside the loop

26. What is break statement?
->  It is a transfer statement. It transfer the control from inside loop or switch outside the loop;

27. What is continue statement?
->  It is a transfer statement. It skips current iteration and starts from next iteration. Used only in loops.

28. What will happen if we don’t write the main method?
->  Program will not execute.

29. Can we run a Java program without class?
->  Fundamentaly it is not allowing but by using some newer features we can do that.

30. What happens if we don’t compile a Java program?
->  It will not execute by the JVM because compilation process is necessory to convert java file to byte code
    which can be understand by JVM and run.

31. Can a variable change its data type?
->  No variable cannot change its datatype bacause in java every variable is assigned a fixed datatype at compile time.

32. What is the size of int in Java?
->  4 Byte.

33. Why Java does not support pointers directly?
->  Because pointers makes code more complex. Pointers gives acccess to the memory so to prevent it and for safety.

		
		
